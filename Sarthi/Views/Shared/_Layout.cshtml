
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Sarathi Parivahan</title>
    <!-- <link rel="stylesheet" href="style.css"> -->


    <style>

        /* Reset some browser defaults */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }

        /* Header Styles */
        .header-bar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px 20px;
            background-color: #f2f2f2;
            border-bottom: 2px solid #ccc;
        }

        .logo img, .logo-right img {
            height: 60px;
        }

        .title {
            text-align: center;
        }

            .title h2 {
                color: #003366;
                font-size: 18px;
            }

            .title h3 {
                color: #555;
                font-size: 14px;
            }

        /* Top Bar Styles */
        .top-bar {
            display: flex;
            justify-content: space-between;
            background-color: #0073e6;
            color: white;
            padding: 8px 20px;
            font-size: 14px;
        }

        /* Navigation Bar Styles */
        .menu-bar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background-color: #003366;
            padding: 10px 20px;
            flex-wrap: wrap;
        }

        .nav-left {
            list-style: none;
            display: flex;
            flex-wrap: wrap;
        }

            .nav-left li {
                color: white;
                margin-right: 20px;
                font-size: 14px;
                cursor: pointer;
                transition: color 0.3s ease;
            }

                .nav-left li:hover {
                    color: #ffcc00;
                }

            .nav-left button {
                background-color: orange;
                border: none;
                padding: 5px 10px;
                color: white;
                font-weight: bold;
                cursor: pointer;
                border-radius: 4px;
                font-size: 14px;
            }

        .nav-right {
            display: flex;
            gap: 10px;
        }

        .login-btn,
        .register-btn {
            background-color: #28a745;
            color: white;
            border: none;
            padding: 6px 12px;
            font-weight: bold;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

            .login-btn:hover {
                background-color: #218838;
            }

        .register-btn {
            background-color: #17a2b8;
        }

            .register-btn:hover {
                background-color: #138496;
            }

        /* Grid Tile Container */
        .grid-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            padding: 30px;
            background-color: #f9f9f9;
        }

        /* Individual Tile */
        .tile {
            text-align: center;
            background-color: white;
            padding: 20px;
            border-radius: 10px;
            border: 1px solid #ddd;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.05);
            transition: transform 0.2s, box-shadow 0.2s;
        }

            .tile:hover {
                transform: translateY(-5px);
                box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            }

            .tile img {
                height: 50px;
                margin-bottom: 10px;
            }

            .tile p {
                font-size: 14px;
                color: #333;
            }

        /* Footer */
        footer {
            background-color: #003366;
            color: white;
            text-align: center;
            padding: 10px;
            font-size: 14px;
        }


        .modal {
            display: none;
            position: fixed;
            z-index: 999;
            padding-top: 100px;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            overflow: auto;
            background-color: rgba(0,0,0,0.4);
        }

        .modal-content {
            background-color: white;
            margin: auto;
            padding: 20px;
            border: 1px solid #888;
            width: 90%;
            max-width: 400px;
            border-radius: 8px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.3);
        }

            .modal-content h2 {
                text-align: center;
                margin-bottom: 20px;
                color: #333;
            }

            .modal-content label {
                display: block;
                margin: 10px 0 5px;
            }

            .modal-content input {
                width: 100%;
                padding: 8px;
                margin-bottom: 15px;
                border: 1px solid #ccc;
                border-radius: 4px;
            }

            .modal-content button {
                background-color: #0073e6;
                color: white;
                padding: 10px;
                border: none;
                border-radius: 4px;
                width: 100%;
                font-size: 16px;
                cursor: pointer;
            }

                .modal-content button:hover {
                    background-color: #005bb5;
                }

        .close {
            color: #aaa;
            float: right;
            font-size: 24px;
            font-weight: bold;
            cursor: pointer;
        }

        .password-wrapper {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .toggle-password {
            padding: 5px 10px;
            cursor: pointer;
            background-color: #ddd;
            border: none;
            border-radius: 4px;
        }

        .password-inline {
            display: flex;
            align-items: center;
            gap: 10px;
        }

            .password-inline input[type="password"] {
                flex: 1;
                padding: 8px;
                font-size: 14px;
            }

        .show-pass-label {
            font-size: 14px;
            cursor: pointer;
            white-space: nowrap;
        }
    </style>


</head>
<body>
    <header>
        <div class="header-bar">
            <div class="logo">
                <img src=~/Content/ministry-nic-logo.png alt="Gov Logo">
            </div>
            <div class="title">
                <h2>MINISTRY OF ROAD TRANSPORT & HIGHWAYS</h2>
                <h3>Government of India</h3>
            </div>
            <div class="logo-right">
                <img src="~/Content/parivahan-logo.png" alt="Sarathi Logo">
            </div>
        </div>
        <div class="top-bar">
            <div>
                <strong>Motor Vehicles Department, Government of Maharashtra</strong>
            </div>
            <div class="datetime">
                DATE: <span id="date"></span> | TIME: <span id="time"></span>
            </div>
        </div>
        <nav class="menu-bar">
            <ul class="nav-left">
                <li>Learner Licence</li>
                <li>Driving Licence</li>
                <li>Appointments</li>
                <li>Upload Document</li>
                <li>Fee Payments</li>
                <li>Others</li>
                <li><button class="status-btn">Application Status</button></li>
                <!-- <li><button class="grievance-btn">File Your Grievance</button></li> -->
            </ul>
            <div class="nav-right">
                @Html.ActionLink("Login", "Login", "Home", null, new { @class = " login-btn" })

                @Html.ActionLink("Register", "Register", "Home", null, new { @class = " register-btn" })

            </div>

        </nav>

    </header>

    <div class="container body-content">

        @RenderBody()
    </div>




    <footer>
        <p>@DateTime.Now.Year Designed, developed by Suraj & Dinesh</p>
    </footer>

    <!-- <script src="script.js"></script> -->



    <script>

        function updateDateTime() {
            const now = new Date();
            const date = now.toLocaleDateString('en-IN');
            const time = now.toLocaleTimeString('en-IN');
            document.getElementById("date").textContent = date;
            document.getElementById("time").textContent = time;
        }

        // Update every second
        setInterval(updateDateTime, 1000);
        updateDateTime();


        document.querySelector('.login-btn').addEventListener('click', () => {
            document.getElementById('loginModal').style.display = 'block';
        });

        document.querySelector('.register-btn').addEventListener('click', () => {
            document.getElementById('registerModal').style.display = 'block';
        });

        function closeModal(id) {
            document.getElementById(id).style.display = 'none';
        }

        // Optional: close modal if user clicks outside
        window.onclick = function (event) {
            const login = document.getElementById('loginModal');
            const register = document.getElementById('registerModal');
            if (event.target == login) login.style.display = "none";
            if (event.target == register) register.style.display = "none";
        };

        function togglePassword(inputId, toggleBtn) {
            const passwordInput = document.getElementById(inputId);
            const type = passwordInput.getAttribute("type") === "password" ? "text" : "password";
            passwordInput.setAttribute("type", type);
            toggleBtn.textContent = type === "password" ? "Show" : "Hide";
        }



    </script>



    <!-- Login Form Modal -->
    <div id="loginModal" class="modal">
        <div class="modal-content">
            <span class="close" onclick="closeModal('loginModal')">&times;</span>
            <h2>Login</h2>
            <form>
                <label for="loginEmail">Email:</label>
                <input type="email" id="loginEmail" required>

                <label for="loginPassword">Password:</label>
                <div class="password-inline">
                    <input type="password" id="loginPassword" required>
                    <label class="show-pass-label">
                        <input type="checkbox" onclick="document.getElementById('loginPassword').type = this.checked ? 'text' : 'password'">

                    </label>
                </div>




                <button type="submit">Login</button>
            </form>
        </div>
    </div>

    <!-- Register Form Modal -->
    <!--<div id="registerModal" class="modal">
        <div class="modal-content">
            <span class="close" onclick="closeModal('registerModal')">&times;</span>
            <h2>Register</h2>
            <form>
                <label for="registerName">Name:</label>
                <input type="text" id="registerName" required>

                <label for="registerEmail">Email:</label>
                <input type="email" id="registerEmail" required>

                <label for="registerDOP">Date Of Birth:</label>
                <input type="date" id="registerDOP" required>

                <label for="loginPassword">Password:</label>
                <div class="password-inline">
                    <input type="password" id="loginPassword" required>
                    <label class="show-pass-label">
                        <input type="checkbox" onclick="document.getElementById('RegisterPassword').type = this.checked ? 'text' : 'password'">

                    </label>
                </div>




                <button type="submit">Register</button>
            </form>
        </div>
    </div>-->


</body>
</html>